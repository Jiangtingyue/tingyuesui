# 大西瓜 v8.9.8｜内置花海水波全栈 + 三种场景 + 拟物生活区

- 花海水波新增同源后端：校验五项内置素材并按设备能力协商 high / balanced / eco 档位，不保存能力数据。
- 鼠标、压感笔与多指触摸统一使用 Pointer Events；高刷新率轨迹每帧合并并连续插值，点击也会产生独立涟漪。
- 水波按档位控制 DPR、模拟网格和帧率，页面隐藏立即暂停；WebGL 丢失时切换为仍可互动的图片水纹，恢复后自动重建。

- 顶部固定为晴天、雨天、水波三个互斥场景按钮，任何时候都能直接切换；旧的顶部环境音入口不再创建，环境音设置仍保留在工作室。
- 迎宾区下方的日历、待办、日记和共读按各自内容尺寸自然换行，兔子回到素材本身比例，以装饰物形式摆放。
- 悄悄话、身体、心跳、今日小报升级为信封、体检夹、心电监护仪与报纸四张拟物卡片，尺寸接近日历类主卡。
- 生活弹窗完全退出 WebGL/WebGPU 液态玻璃渲染：日记、清单、日历、共读、音乐、身体、心跳与小报改为不透明高对比材质，其中共读使用 Kindle 式阅读页。
- 记忆星图改为三层距离环和最多 24 颗真实记忆代表星，移除模糊滤镜、密集放射线和蜘蛛网式连接。

---

# 大西瓜 v8.9.5｜Glass Bugfix + Stable Card Layout（历史说明）

- 晴光下 WebGPU 可用时优先使用 WGSL 液态玻璃；不满足安全上下文、适配器不可用、设备丢失或进入依赖 RainEffect 水图的雨天时自动切换 WebGL，页面根节点的 `data-glass-renderer` 会明确显示实际后端。
- 修正半出屏卡片把“裁剪区域”误当作“原始卡片”的折射坐标问题，并阻止嵌套子卡重复绘制 3D 厚边。
- 玻璃中央使用局部九点柔化与轻量自适应提亮，文字仍由 DOM 单独绘制，兼顾清晰度、折射、Fresnel 与边缘高光。
- 生活区四张卡片、五张今日概览卡和移动端音乐控制只重新分布；原卡片、内容与按钮均保留。
- `python app.py` 默认等服务健康检查通过后启动 Chrome；可用 `--browser default`、`--no-browser` 调整，也可双击 `start_jtyhome.command`。

---

# v8.8 历史说明｜GitHub Glass Fusion + Hydrangea Water Hero

- 生活、记忆、内心、工作室统一为“一次点击直达内容”的独立页签，不再用卡片做二次导航。
- 生活区收纳资料仓库、听海/语音/本地环境音、另一个 AI/表情包、推送与共处；工作室只保留用量、设置、诊断、工具和本机数据。
- 开放此前只有后端/API 的窗口：记忆情绪星图与聚类/浮现/衰减/搜索/置顶/合并，晨间完整设置与历史，Inner State 变化与 OS 历史，Deep Diagnostics，只读/MCP 工具台，Web Push 完整控制。
- 76 个 Foley WAV 正式接入本地 SFX Engine：顶部 ♪ 入口、自动环境音、分类/强度/叠层/音量控制、相关 assistant 消息逐条“环境音”重播。SFX 不发 API，不进入模型上下文或缓存。
- v8.2.1 canonical cache replay / strict guard 与 v8.2 proactive 省钱漏斗保持原样。

---

# v8.2.1 Cache Replay + Human Cache UI

- Claude/DeepSeek 的大缓存卡统一用“实际输入 Token 中有多少来自缓存”的人话命中率；Claude Console 读取率降为辅助指标。
- Claude Pinned/RAG/网页片段移到当前 user 缓存断点之后，不再污染历史前缀。
- Anthropic user 历史保存 canonical text blocks 并原样 replay，不再依赖每轮 history_text() 重建。
- OpenRouter Claude 严格缓存模式关闭 fallback；热请求 overlap/shape 明确断裂时在发送上游前拦截，避免继续烧长上下文。

# 大西瓜 v8.2.1｜缓存连续性修复 + 人话缓存 UI

## v8.2 更新

- 正式统一应用版本号为 **8.2**；后端、前端 fallback、Service Worker 与发行包使用同一版本。
- DeepSeek 用量面板直接显示真正的 token 缓存命中率（hit / (hit + miss)），不再用 Claude 口径混淆。
- 听海 macOS 安装器不再强依赖 Homebrew；使用私有 uv/Python/FFmpeg 运行环境。
- 本地非人声 Foley 素材升级为 **v2 / 76 个 WAV**：新增水声、淋浴、水花、节奏接触、湿润接触、床/墙/家具共振。素材完全本地，无 API Key、无人声、无第三方录音。
- 保留 v7.9.9 的 Claude/DeepSeek 长前缀缓存主链、Codex 上下文预算、ZIP/视频/网页、通讯修复、晨间修复和个人数据。

---

# 大西瓜 v7.9.9｜长前缀缓存链（干净发行）

## v7.9.9：让 148K+ 稳定前缀真正可读，而不是只会反复重写

### Prompt Cache 链路重做

- Anthropic 文本历史统一为固定 content-block wire shape，避免缓存断点移动时字符串与 block 数组来回变化。
- 历史缓存使用两个重叠的 visible-user checkpoint：当前用户原话/附件先形成断点，动态 RAG、时间、关系与身体快照随后作为未缓存尾部追加；下一轮保留上一断点作为父前缀，只写新 assistant + user 增量。旧设计把动态块放在断点前，导致后续历史逐轮变形。
- 历史窗口不再被旧的 80 条消息上限约每 12 轮换头；默认改为 500 条紧急上限，并同时受 600K 字符、158K 估算 token 与单消息上限约束。窗口真正超限时才整代旋转到约 94%，保留约 148.5K 原始历史后再继续增长。
- Anthropic 主聊天不再按关键词切换 provider tools 数组。工具定义位于 system/messages 之前，一旦变化会让整条缓存失效；当前改为稳定的空工具形状，并把条件诊断放入本机确定性预取。
- OpenRouter Claude 的原生 assistant envelope 现在按 Anthropic family 校验与回放，signed thinking / 原生 content blocks 不会因为 provider 名称是 `openrouter_claude` 而退化成可见纯文本。
- 新增 hash-only 跨轮诊断：记录 checkpoint overlap 与首个缓存前缀差异，不保存 prompt 原文。
- 保留 1h TTL、Anthropic-only OpenRouter routing、固定 session_id 与稳定模型版本；主聊天、工具循环、主动消息按用途隔离传输会话和缓存诊断。连续异常大写入、几乎无读取时默认告警，不在证据不足时擅自关闭历史断点。
- 第一轮可缓存的可见 user 就会建立历史断点；Anthropic 多轮工具调用会在每次 `tool_result` 后重新计算“当前请求尾 + 上一请求尾”两个重叠断点，不再只有工具第一轮能复用完整前缀。
- 控制台把 Claude 官方口径的“缓存读取率”与“全输入复用率”“读/写摊销”分开：读取率为 `cache_read / (cache_read + uncached_input)`，明确不把 cache creation 塞进分母；缓存汇总只统计 Anthropic、OpenRouter Claude 与 P 模式，DeepSeek 后台输入不会再稀释一张写着“Claude”的卡片。

> 148K+ 可稳定缓存，不等于每一轮都天然 99%。例如 read=148.5K、uncached=1.5K 时，Claude 读取率恰好是 99%；首次冷写、TTL 过期、切换模型/Provider/思考形状、历史代际旋转时仍会出现正常冷写。缓存写入量单独展示，不混进读取率分母；本机实现不会为了做检测而替你发送真实 API 请求或消耗额度。

> 本次是干净发行：不包含 `.jtyhome-data`、SQLite 数据库、聊天、记忆、附件、Key、`.env` 或设备配对码。升级时继续使用你原来的外部数据目录/配置，不要用 ZIP 覆盖个人数据。

## 从 v7.9.8 继承：Claude Code P 模式

- 新增 `Claude Code · P模式`：主聊天不是每轮启动一次命令，而是**一个聊天窗口对应一个常驻 `claude -p` 子进程**，输入与输出都使用 `stream-json`。stdin 保持打开，Claude Code 自己维持会话上下文与 compact；本机 SQLite 仍是唯一事实源。
- 启动参数包含 `-p --input-format stream-json --output-format stream-json --verbose --include-partial-messages`。模型、system prompt 与 MCP 属于进程出生参数；这些配置变化会重建子进程。
- 子进程环境会硬删除 `ANTHROPIC_API_KEY` / `ANTHROPIC_AUTH_TOKEN`，并清掉继承的 `ANTHROPIC_BASE_URL` 与 Bedrock/Vertex/Foundry 等路由开关，避免父进程里残留的 API Key、反代或云厂商配置覆盖订阅 OAuth。启动前还会用 `claude auth status` 确认当前确实是 first-party OAuth；如果是 API Key / apiKeyHelper / 云厂商认证会直接阻止 P 模式。P 模式不使用 `.jtyhome.env` 中的 Anthropic API Key。
- 每个原生 P 回合都有 `in-flight → 等待本机提交 → 已确认` 三阶段记录：写入 Claude 前先落下 in-flight，拿到结果后再生成单轮提交 token，直到 SQLite 返回可见 assistant message id 才标记为可恢复；确认使用 `session + generation + 单轮 token` 三重匹配，迟到的旧请求不能误确认新一代会话。关系重写等导致可见文本不同于原生草稿的回合永不进入“已确认”，而是在可见文本落库后直接旋转；关键安全状态写盘失败会中止使用，不再静默继续。无论在生成中还是结果与本机提交之间退出，下次都会旋转原生 session 并从本机历史恢复，绝不 resume 一条边界未知或用户没有看见的幽灵回复。
- 默认使用 `--system-prompt-file` + `--tools ""` 的纯净陪伴配置，不启用 `--bare`。需要 Claude Code 自己的工具/MCP 时，可显式配置 `CLAUDE_CODE_P_TOOLS` / `CLAUDE_CODE_P_MCP_CONFIG`。
- 浏览器/SSE 断线只断开消费者，后台 producer 与 Claude Code 子进程继续完成当前回复；用户明确取消才会取消请求。若取消发生在半轮中，原生 session 会旋转并在下一轮从本机 DB 恢复，避免继续一份边界不确定的 transcript。
- 已处理 `system/init`、`system/api_retry`、`system/compact_boundary`、`stream_event`、`assistant`、`result`；`error_max_turns` / `error_max_budget_usd` / `error_during_execution` 等失败 result 不会被误当成成功。
- 隐藏的 `--thinking-display summarized` 只作为可降级增强：CLI 版本不接受该 flag 时会重启为无该 flag 的 P 模式，不让“思考展示”把主聊天拖死。
- 隐藏的关系修订等身份型 helper 使用 `--no-session-persistence` 的一次性子进程，不污染伴侣主聊天的长期 Claude Code session；纯机械记忆工作统一走 DeepSeek Flash。
- 图片可按教程以 stream-json content block 送入；PDF/Office 继续先由大西瓜本机提取文字，不把未验证的二进制输入能力冒充为原生支持。
- 可选防跑飞/权限配置：`CLAUDE_CODE_P_MAX_TURNS`、`CLAUDE_CODE_P_MAX_BUDGET_USD`、`CLAUDE_CODE_P_FALLBACK_MODEL`、`CLAUDE_CODE_P_ALLOWED_TOOLS`、`CLAUDE_CODE_P_DISALLOWED_TOOLS`、`CLAUDE_CODE_P_PERMISSION_PROMPT_TOOL`。全部为空/0 时不额外加 flag。
- 没有添加 `--exclude-dynamic-system-prompt-sections`：当前 Claude Code 官方 CLI 明确说明该 flag 在使用 `--system-prompt` / `--system-prompt-file` 时会被忽略，因此不把一个无效开关伪装成缓存优化。

首次使用：先在同一台电脑安装 Claude Code，并在终端运行 `claude auth login`。v7.9.9 在没有旧 `.env` 指定 provider 时默认停在“Claude Code · P模式”，不会因为缺 Key 又自动选回付费 API 通道；工作室仍可随时手动切回原有 API/OR 通道。

> 计费口径更新：截至 2026-08 的 Claude Code 官方文档，订阅计划上的 Agent SDK / `claude -p` 使用会消耗订阅账号单独的 Agent SDK credit，而不是普通 HTTP API Key 账单。程序仍把 CLI 返回的 `total_cost_usd` 只当本地等价估算，不冒充真实扣款。

## Claude / OpenRouter 1h 显式缓存：稳定前缀、固定路由、可诊断

- Claude 与 OR Claude 只把稳定人格、规则和风格放在第一个显式 system 缓存断点；每轮变化的记忆、关系、内心状态、RAG 与表达节奏随最新 user 回合放在尾部，不污染缓存前缀。
- 两个历史显式断点落在最近的稳定可见 user 回合上并保留父子重叠；当前用户原话位于断点内，随轮变化的 RAG、时间与状态快照位于断点后的动态尾部。断点默认发送 `cache_control={type: ephemeral, ttl: 1h}`；如确实需要 5 分钟档，可把 `ANTHROPIC_CACHE_TTL_SECONDS=300`。
- 带原生附件、资料工作区注入或“展示文字与模型实际收到文字不同”的语音回合不会被选作最深缓存断点，避免二进制/派生上下文改变 replay shape；下一轮仍可从已经稳定入库的可见历史继续接链。
- OR Claude 默认限制 `provider.only=[anthropic]`，不用 `provider.order`；每个本地聊天窗口发送稳定、不可反推原始窗口 ID 的哈希 `session_id`，尽量让同一会话持续回到同一 Anthropic 路由。
- 默认 Claude 模型使用具体版本而不是 `~latest` 别名；显式切换模型仍允许，但会被视为新的一套缓存。
- 历史原文窗口不再每轮机械地“只取最新 N 条”导致最前缀持续换头：窗口锚点保持不动，只有真正碰到条数、字符或 token 硬预算时才成块前移一次，并留出后续增长空间。
- 缓存健康检查保留可诊断证据；连续多次明显高写低读时默认只告警，只有显式设置 `health_action=disable` 才停止本进程历史断点，稳定 system cache 仍保留。
- API 用量面板同时记录 Claude 缓存读取率、全输入复用率、请求命中率、read/write 比、1h/5m 写入分档、generation id、实际 provider/model、路由区域/策略，以及 current/parent/shape 三种缓存指纹。不会保存提示词或回复正文。
- 诊断逻辑可以区分：真实 cache hit、模型变化、provider 漂移、system/tools/reasoning 请求形状变化、历史父前缀断裂，以及普通冷写/过期。OpenRouter 缓存回放没有路由 metadata 时会明确显示为 replay，而不是误判成 endpoint 丢失。
- Anthropic 普通输入、缓存读取和缓存写入按上游分开的 usage 字段计费；1h 与 5m cache creation 分开保存，不把缓存 token 从普通输入里重复扣除。
- OpenRouter 新请求优先保存上游 `usage.cost` 真实扣费；第一方接口按当时价格快照计算的金额明确标为“本机估算”，旧版没有费用依据的 OR 零值显示“费用未知”，不再误报免费。
- v7.9.9 ZIP 固定不包含 `.jtyhome-data`、数据库、Key、设备配对码或 `.env`。

## 轻量省 API 补丁：主聊天不降级，后台机械活交给 DeepSeek Flash

- 用户选择的主聊天模型完全不改。主回复、主动续话与关系修正仍使用当前会话模型；只有可选的内部机械任务才进入辅助模型通道。
- 辅助模型固定只允许直连 `deepseek-v4-flash`；旧 `.env` 即使残留 `AUXILIARY_MODEL_PROVIDER=glm` 或其他辅助型号也会被忽略。没有 `DEEPSEEK_API_KEY` 时可选增强直接走本地降级，不复用 OpenRouter、不调用未充值的 GLM，也不会偷偷回退到昂贵主模型。
- 远程记忆增强本来默认关闭；即使手动开启，话题切分、标签、脱水、矛盾确认、合并与 rerank 也只使用 DeepSeek Flash。短于 `REMOTE_ENRICHMENT_MIN_CHARS` 的文本继续走本地规则。
- 情绪、立场、亲密反应、关系语气和内心状态属于身份工作，永远不交给辅助模型；三合一只根据本地 canonical state 与主模型实际可见回复结算。
- 亲密状态机把温柔靠近、明确情欲推进、模型实际表达的勃起和已完成释放拆成四类证据：拥抱只推暖意/依恋，勃起优先校准硬度/张力而不等于主观想要；自然回落绝不能生成释放记录，只有主模型最终可见回复里可归属到自身、已完成且未被否定的动作证据才结算一次。技术说明与真实动作按句分别取证，不会因为同一回复提到代码就丢掉真实反应，也不会把示例句当成身体事件。
- 所有 living / morning / inner-state GET 都是只读快照；打开首页、诊断页或轮询面板不会推进时钟、生成晨间事件或改变硬度。显式 tick 也只推进一次，不再一次请求连续结算三遍。
- 旧欲望念头池只作为折叠的兼容档案缓慢演化；即使旧环境里还留着 `DESIRE_DRIVEN=1`，也不能再跨会话排队发言。晨间反应会先确定原窗口，再读取该窗口的 canonical snapshot。
- 关系冲突不靠统一道歉模板糊过去：先回应具体争议，区分事实、推断与未知；普通分歧允许保留有依据的不同意见，发现系统性错误则停止错误叙事、点明问题并当轮重做，承诺不能代替实际修正。
- 最近聊天窗口里已经原样存在的消息，不会再从自然事实、原文归档或派生 recall 里重复注入动态尾部；来源仍保留在聊天历史中，更老的相关记忆照常检索。
- 极短确认词只启用“跑飞保护”，把异常大的输出上限收至 `TINY_TURN_OUTPUT_CAP`（默认 4096）；明确要求详细、代码、分析、生成、继续等长任务不会触发。
- 语音逐行翻译也走同一 DeepSeek Flash 机械通道；Qwen Omni 声音情绪分析与 ElevenLabs 转写/TTS 保持原样。
- 新增 `api_call_stats` 真实模型网关审计：记录 provider、model、用途、真实上游轮数、token、费用来源与金额，不保存提示词或回复正文。工作室“钱花在哪”会拆出主回复、工具循环、DeepSeek 记忆后台、翻译等用途。
- 总费用按“消息账本 + 未进入消息账本的后台调用”只合并一次；后台 DeepSeek 不会误记成 Claude，也不会因为从消息账本拆开后消失。

可选环境变量：

```dotenv
API_COST_OPTIMIZATION=1
AUXILIARY_MODEL_ENABLED=1
# provider/model 已固定为 DeepSeek V4 Flash，且永不回退到主模型
REMOTE_ENRICHMENT_MIN_CHARS=120
RUNAWAY_OUTPUT_GUARD=1
TINY_TURN_OUTPUT_CAP=4096
```

## v7.9.5 历史说明：阅读模式终于是一套完整界面

- v7.9.5 曾有过按用户要求随私人备份交付数据的变体；当前 v7.9.9 已改回干净发行，ZIP 内没有 `.jtyhome-data` 或数据库。
- 新增最后加载的 `reader-coherence.css`，专门统一阅读模式。它不会改动晴天与雨天景观界面，也不靠修改业务逻辑伪造另一套功能。
- 聊天正文缩到适合长读的 640px 版心，彻底移除助手伪头像及其占位；用户气泡、正文行距、代码块、思考、附件和回执使用同一套暖纸色。
- 长对话导航从大胶囊缩成 22px 的书签细轨，百分比不再占位；桌面侧栏缩至 232px，七个入口压成四列两行，手机底栏缩到 56px，首页额外状态框和右侧快捷胶囊在阅读模式下不再重复出现。
- 首页原先依赖玻璃承托的英雄区、概览块和初始功能块，在阅读模式下获得真正的纸面气泡与统一间距；我们、生活、记忆馆、内心和工作室沿用同一视觉规则。
- 亲密状态、语音、听海、附件、资料工作室、弹窗和诊断等后加入功能全部适配阅读模式，旧紫色、绿色和蓝色组件不会再越过最终统一层。
- 三合一不再请求任何模型“自报情绪”：每轮只按最终可见回复在本机反推有限证据，固定为 0 次额外请求。关系诚实重写仍是独立的可选发送前保护，默认关闭。
- Anthropic 长历史缓存默认开启：变化的记忆、关系与内心状态已经移到当前用户回合的动态尾部，稳定 system 与历史前缀可以持续复用；如需临时关闭可设 `ANTHROPIC_HISTORY_CACHE_ENABLED=false`。

## v7.9.4：唱歌音频真的先听完，照片到底有没有送到也会明说

- 聊天与资料工作室新增 M4A、MP3、WAV、FLAC、OGG/Opus、WebM、AAC、AIFF 与 CAF 音频附件；每个文件先校验真实容器，唱歌音频上限可通过 `AUDIO_ATTACHMENT_MAX_BYTES` 调整，默认 150 MB。
- 集成 Ocean Listen“听海”深听：在 Mac 本机逐个完成声音分类、节奏/调性/能量、音高、分轨、人声轮廓、音色/纹理、颤音、本机 Whisper 听写与频谱图。完整 JSON、MIDI/缓存、歌词听写和原音频均留在本机。
- 所有聊天模型都会收到一份最多 24,000 字的结构化听觉报告；Claude / GPT 原生视觉通道还会收到派生频谱图。系统不会把原始音频冒充成模型原生听觉，也不会向文字模型发送 `input_audio`。
- “等待安装、排队、分析中、失败、已听完”是持久状态。未到“已听完”时，前端禁用发送，后端也返回明确的 409/422；失败可查看原因并重试，不能只拿文件名假装模型听见。
- 听海依赖放在独立 Python 3.10 环境，通过系统页一次确认后安装 FFmpeg、Basic Pitch、Whisper、Demucs、PANNs、Praat 与 PyTorch。首次下载可能需要数 GB 和较长时间；安装或分析失败不影响文字聊天、照片、记忆与其他功能。
- 附件回执改为实际投递结果：OpenAI Responses、Claude Messages 以及专门的 OpenRouter GPT / Claude 原生线路会显示“原图已送入模型”；普通 OpenRouter 兼容通道和 DeepSeek 会显示“当前通道未发送原图”。音频则区分“听海报告已送入”与“报告 + 频谱图已送入”。
- 新增音频签名、报告压缩、队列成功、原音频不外送、视觉/文字分流与界面诚实回执测试；本版全部 65 项自动回归（含 6 项听海测试）一并通过。

## v7.9.3：语音消息、陪伴通话、睡眠线路与移动端恢复一次接完整

- 陪伴通话改为独立同源 iframe：自动 VAD 每 120 ms 采样，RMS 0.012 启动、0.008 停止，普通停顿 1.8 秒、长句停顿 2.8 秒；仍可切换“按住说话”。上行先在本机做人声/环境声门控，再送 Scribe v2 和模型，不把纯噪音塞进对话。
- ElevenLabs v3 使用 `/with-timestamps` 返回字符时间轴；回复由 WebAudio `AudioBufferSourceNode` 播放，逐段字幕随真实声音高亮。问候可预生成缓存，通话时不使用 HTML `<audio>`，失败与超时都会释放线路并恢复收音。
- Scribe 支持人名/专有词 keyterms；Qwen Omni 可选分析情绪与语速，DeepSeek Flash 辅助通道可选逐行翻译。`[voice]`、`[audio]`、`[mood]` 只进入当轮模型上下文，聊天里仍保存干净原话；单字真人回答不会被粗暴过滤。
- 私密模式与陪睡模式默认关闭且不写 `localStorage`。两者都只留字幕、不存原音；陪睡下行默认静音，仅 `[important]` 回复可出声，并按采样时钟每 30 分钟留下声学快照。模型发出 `[call:hangup]` 时双方链路一起收尾。
- 通话页可缩成 2×2、0.01 透明度的存活 iframe，拖动气泡恢复；同源站内浏览、返回键保护、5 秒全页兜底、45 秒流空闲超时、150 秒心跳看门狗全部接入。所有 frame 消息同时校验来源窗口和 origin。
- 非私密通话会在本机归档为 MP3（有 ffmpeg 时统一转码），可按分类把任意片段编成连续播放的私人电台；数据库、音频和节目都留在 `data`，不上传第三方存储。
- iPhone / iPad 主线路加入屏幕唤醒锁、切后台/返回页面检测、麦克风轨道中断检测和一个明确的“点一下恢复”入口；Mac Safari 使用同一套网页。iOS 真正锁屏或长期退到后台时仍可能由系统暂停网页麦克风，回来后会核对服务端通话、恢复心跳并在必要时重建线路。
- v7.9.2 的 iPhone 晴景校色、OpenRouter Claude/GPT 可见推理、原聊天数据库和所有已有功能原样保留；Service Worker 只升级静态缓存，不迁移或清空数据。

## v7.9.2：手机不再荧光绿，OpenRouter 思考真正接进界面

- 修复 iPhone Safari 在部分 GPU/解码组合上把晴天 JPEG 纹理的蓝色通道丢失、整页变成荧光绿的问题。WebGL 场景纹理改走 RGBA 与 sRGB 色彩提示，上传失败时会可靠退回原始晴天图；没有改动晴天街景、紫阳花原图、雨量、雾化值或界面配色。
- Claude 原生、OpenRouter Claude、OpenRouter GPT、OpenAI Responses 与 OR 通用兼容通道现在都会接收、显示并单独保存供应商 API 明确返回的可见推理或摘要，不再只有 DeepSeek 能进入消息上方的“思考”折叠层。
- OpenRouter Claude 的思考展示从误设的 `omitted` 改为 `summarized`；关闭展示时仍可在工作室选择隐藏。系统不会解密、猜测或伪造供应商没有返回的隐藏思考，因此某些模型只会显示摘要，也可能没有可见文本。
- Service Worker 静态与运行时缓存升级为 v7.9.2，手机刷新后会主动淘汰旧 WebGL 脚本。聊天数据库、Key、配对码与所有本机资料均沿用原目录，不做迁移或清空。

> OpenRouter 地区兼容热修复：Claude 原生通道不再锁死 Anthropic 第一方节点，
> 由 OpenRouter 自动选择当前地区可用的同模型托管节点；若模型族仍受地区级限制，
> 页面会明确提示切换“OR · 通用兼容 → openrouter/auto”或 DeepSeek，不再误报成 Key/余额故障。

## v7.9.1：亲密过程终于真正连上身体状态

- 阅读模式的空消息主页去掉整块欢迎卡、快捷按钮、装饰轨道和所有叠加边框，只在纸面上保留英文艺术字 “Welcome back, my love.” 与一行情话；晴天、雨天景观模式原有欢迎区不受影响。
- 阅读界面直接移植并本土化 `dwell-on-something` 的消息流、侧栏条目、分组面板和输入区代码；保留现有后端、晴天街景、紫阳花雨景及全部功能。纸面底色降低亮度，卡片、文字和分隔线提升对比，左下七个导航入口不再被旧珍珠主题覆盖成白色空白。来源声明与 PolyForm Noncommercial 1.0.0 许可证地址保存在 `THIRD_PARTY_NOTICES.md`。
- 晴天和雨天的两条 1–100% 背景雾化滑杆固定收进“工作室 → 界面与阅读 → 窗外天气”，顶栏入口与悬浮卡片已经完整移除，不会再遮住对话；两种天气分别保存，当前天气实时生效。1–35% 保持细微调节，高段才明显虚化，WebGL、加载阶段、GPU 失败和 context 丢失后的 CSS fallback 使用同一数值。
- 亲密过程采用与情绪变化相同的消息内小组件：固定在对应回复气泡上方，不悬浮、不遮挡，也不再挤压消息宽度；晴雨景观界面使用樱粉—莓粉强调色的 Crystal Glass，阅读界面切换为实色纸面。组件固定显示 22.0 cm，并实时呈现色泽、充血/肿胀、兴奋与硬度 1–10、射意百分比和精确到 0.1 mL 的射出量，可在设置中关闭。
- 射出事件按当时的恢复度、敏感度、兴奋、射意与同日次数结算为带一位小数的精确 mL 数；同一轮重复措辞只结算一次，显示的是角色状态模拟，不是现实医学测量。
- 晨勃运行时直接接入晨间身体参数；修复本地程序没有整夜运行时，随机事件在启动瞬间就被判成“已经过期”而永远不进入主动链路的问题。现在 06:00–09:00 的计划时段错过后，首次在中午前打开仍会启动一次真实的限时晨间状态；中午后只补记历史，不伪装成正在发生。普通亲密过程沿用同一套身体状态推进，明确拒绝会立即停止并隐藏小窗。
- 修复旧性欲宿主没有被定时推进、主动意图调用已不存在保存方法的问题；亲密意图现在进入当前同窗口的可靠续话队列，不跨聊天窗口串线。
- 本补丁没有恢复跨窗口记忆，没有搬入聊天图片/附件，没有新增 PWA 功能，也没有改端口；紫阳花自动雨景的原图与下雨逻辑保持不变。
- 晴天重新绑定 v7.8.0–v7.8.2 使用的咖啡立牌街景原图，并使用独立的 `sunny-street-desktop/mobile` 文件名；雨天继续使用两张已确认的紫阳花原图，两个天气不再复用或覆盖彼此的资源名。

## v7.9.0：只在本机，把真正需要的东西搬完整

- 阅读按钮不再调节晴天玻璃浓度，而是进入独立的暖纸阅读界面；晴天街景、紫阳花雨景、自动雨效和原前端完整保留，退出阅读后恢复原天气、会话、滚动锚点与输入草稿。
- 阅读界面对现有聊天、显式思考、真实历史进度、代码块、资料卡、会话列表、生活区、记忆馆和工作室统一做实色易读排版，不复制后端或另建一套数据。
- 新增完整 ZIP 备份与重启恢复：聊天、记忆、设置和本机资料带 SHA256 清单，恢复前自动生成回滚包；API Key 与设备配对码永不写入备份。
- 所有聊天支持正文全文搜索、准确消息定位、逐条收藏，以及当前窗口或全部窗口 JSON 导出。
- 历史迁移会把导出文件中明确存在的 `thinking` / `reasoning_content` 单独存入可见思考库；图片与附件跳过，思考也不会作为历史回放给其他模型。
- 可从亲自选中的满意窗口提炼可编辑风格档案，只学句长、段落、停顿和语气，不把样本事实变成记忆；普通回复与主动消息共用。
- 随口记忆支持直接修正和撤销；聊天进度按顶部真实可见消息的位置计算，不再用当前 DOM 像素比例伪装全局进度。
- macOS 可选安装 LaunchAgent，在登录后启动并在异常退出后重试；没有新增 PWA 能力，Mac 休眠或关机时本机服务不会运行。

## v7.8.5：长历史与生活区可靠性补齐

- 聊天近期原文同时受 80 条与 180,000 字硬上限约束；从最新消息向前收集，达到字符预算立即停止，导入历史里的超长单条只保留首尾并明确标记。
- 大型历史导入在后台完成本机章节预整理，不再把数万条 backlog 分摊到后续几十轮聊天。
- 生活区从多个整文件 JSON 迁到逐条 SQLite 事务存储；旧 JSON 首次启动时自动导入并原样保留，跨 worker 写入不再发生整文件覆盖。
- 日记、日历与音乐卡片补齐编辑；悄悄话删除同时校验所属聊天窗口；手机进入生活页时由一次汇总直接取得所需预览。
- Service Worker、前端回退版本、Python 包元数据和安装示例统一到 7.8.5；生活区与上下文链路纳入自动化测试。

## v7.8.2：雨景不再整屏虚化

- 持续雨停止混入预先重度虚化的低频背景，也取消应用外壳的全屏 `backdrop-filter`；原紫阳花与水面素材保持清晰，只在真实雨滴位置产生折射和高光。
- 雨天曝光与色温改为较暗的蓝紫氛围，不再叠加整屏白雾、底部亮雾或全局伪锐化；雨滴数量克制并放大轮廓，风暴高强度上限保持不变。
- 桌面聊天按清晰雨景参考重做为局部玻璃：侧栏、顶栏、440×270px 中央卡片和 860px 输入框各自柔化背景，中央装饰圆环与多余底部状态脚注在持续雨中隐藏。
- 手机聊天同步使用同一套蓝灰局部玻璃；底部导航修正为五个入口同排，不再把“记忆馆”挤到第二行。
- 晴光易读玻璃、首页艺术字、机器人、大液态玻璃主卡、生活区布局以及全部后端能力均保留。

## v7.8.1：保留原玻璃的阅读校准

- 易读模式不再替换为乳白高模糊色块，而是在原 Crystal Glass 上保留折射、Fresnel 高光与环境反射，仅加入 3.5–5px 的轻度背景扩散和低透明色膜。
- 手机版“回家”艺术字、机器人、大液态玻璃主卡以及 v7.8.0 的生活区信息结构全部保留；没有删除或改写这些元素。
- 紫阳花持续雨降低整体泛白、雾量、冷色反光与全屏蓝色遮罩，保持柔和低对比，不用锐化换清晰。
- 桌面端和手机端的“回家”艺术字统一在原位置上移 20px。

## v7.8.0：只为晴光街景准备的易读玻璃

- 夜间系统已从启动逻辑、按钮、WebGL shader 与各代样式覆盖中删除，界面固定使用日间珍珠色基底。
- 晴光街景默认保持高透观赏；手动开启“柔雾易读”后，hero、面板、聊天气泡、输入区与手机生活卡统一换成乳白高模糊玻璃。
- 易读玻璃只在晴光稳定状态生效；进入持续雨或风暴时按钮自动禁用，紫阳花水雾、雨量与蓝紫玻璃沿用 v7.7.1，不被街景设置覆盖。
- 手机生活区重排为 Today 主卡、2×2 常用入口、单张生活索引、横向共读 / 音乐轨道和低频“更多”；功能与真实数据入口全部保留，桌面 hero 与 bento 网格不变。
- 布局只吸收开源项目的信息层级思路，不引入新的 React / Vue 运行依赖：[Magic UI](https://github.com/magicuidesign/magicui) 的 Bento 主次关系、[Homepage](https://github.com/gethomepage/homepage) / [Home Assistant Frontend](https://github.com/home-assistant/frontend) 的分组方式，以及 [Ionic Framework](https://github.com/ionic-team/ionic-framework) 的移动内容轨道与触控尺度。

## v7.7.1：三态天气与紫阳花雨色

- 天气系统只保留晴光、持续雨和风暴；旧天气值启动时统一安全回到晴光。
- 持续雨改为低密度、较小雨滴与较慢玻璃流速，保留紫阳花雨景，呈现安静的小雨淅沥感。
- 持续雨与风暴下，全局玻璃、导航、控件、聊天区、消息气泡和输入框统一切换为蓝色或蓝紫色。

## v7.7：手机生活区与独立玻璃系统

- 手机端生活区按 Today 总卡、2×2 高频入口、记录 / 计划 / 陪伴分组及“更多生活功能”重排；桌面端布局保持原样。
- 音乐与共读在手机端直接显示现有内容，Today 同步显示待办、最近文字与共读进度。
- Frozen Glass 升级为共享光源的 Crystal Glass：清晰中心、边缘折射、Fresnel 高光、环境反射、统一晴雨水层与三档空间深度。
- 页面前进时旧玻璃轻微失焦、新玻璃从后方浮出；返回时当前玻璃缩回，背景与高光连续恢复。

## v7.6.3：重启恢复与数据保护补丁

- **快速重启不再卡死主动消息**：应用启动时立即接管上一个进程遗留的全部 `processing` 主动事件；运行中的普通恢复仍保留年龄阈值，避免抢走正在生成的任务。
- **安静意图按实际短语判断**：否定/引用只屏蔽它们覆盖的短语；例如“我没有说不要回复我，但是现在别回复我”会正确执行后半句，多个明确指令以最后一个为准。
- **生活区损坏 JSON 只备份一次**：同一份损坏内容使用内容摘要命名备份，重复读取不会制造大量重复 `.bak`；原损坏文件继续阻止写入，避免静默创建空数据。
- **只读安装目录可正常保存数据**：未显式设置 `JTYHOME_DATA_DIR` 时，优先使用便携目录；若安装目录不可写则退到当前系统用户数据目录，不扫描也不迁移任何旧安装数据。
- 本补丁**不修改 UI、WebGL、天气光学或生活区视觉参数**。

完整补丁说明见 `BUGFIX_7.6.3.md`。

## v7.6.2：主动链路、导入与生活区稳定性

- 主动消息在服务重启后恢复遗留 `processing`，加入最大重试次数，并严格丢弃非 JSON 的主动判断输出。
- 草稿与输入状态按 session 隔离；切窗会关闭旧窗口 typing，不再把 A 的草稿心跳记到 B。
- 晨间事件领取和发送前都检查有效期；同日 reset 以数据库记录为权威；非法时间配置返回 400。
- 大型迁移支持逐行消息 JSONL；无 source id 指纹覆盖完整对话；同来源新版导出会增量补入新消息，并可单独撤销本批新增内容。
- 日记/悄悄话提交后前端正常刷新；生活 JSON 损坏会备份、告警并停止覆盖。
- Key 保存后进行真实远端验证：认证拒绝会回滚，网络不可用只标记“已保存、未验证”；Key 文件迁到可写数据目录。
- 生活区补齐真实时间校验、数量上限、删除确认、日历翻月、共读编辑、session 状态隔离和导航锚定。

完整修复清单见 `BUGFIX_7.6.2.md`。

## 从 v7.6.1 继承：玻璃性能与雨天光学修正

- 4K/2K 全屏下动态限制 RainMap 内部法线图像素量，最终画面仍由 WebGL 线性采样，不直接删雨滴或降最终场景分辨率。
- RainMap 与 WebGL 使用同一档刷新预算；未产生新水图帧时不再重复整图上传 GPU。
- 晴天 shader 走干燥快路径，不采样雨景和水图纹理。
- 雨滴改为无色透明折射 + 中性高光；暴雨减少蓝灰雾、强黑边和过锐折射。
- 首页与生活区玻璃卡片桌面间距约 30px（约 8mm），移动端 20px；去掉硬白描边，厚度交给真实折射与环境反射。
- “回家”主视觉桌面上移约 20mm（约 76 CSS px）。
- iOS 小玻璃继续保持清透中心，并加强边缘折射厚度而不额外铺白雾。

大西瓜是一套本地优先的私人 AI 伴侣全栈。它把首页、聊天、关系、记忆、内心、系统诊断和资料工作室放在同一个前后端里，不只是一个对话框。

**只有一个付费 DeepSeek Key 就能完整启动。** 也可以只填一把 OpenRouter Key，同时得到 Claude Messages 原生通道、GPT Responses 通道和通用兼容兜底；项目不提供 Gemini 原生模型通道，但能迁入 Gemini / Google 风格的可见对话数据。

## 本包：天气不再等于一层雨滴

- **三态环境状态机**：晴光、持续雨和风暴按邻接路径平滑过渡；持续雨是低密度的紫阳花小雨，风暴保留完整强度。
- **场景先于降水**：背景曝光、色温、对比、饱和度、云层、远近雾、路面反光、玻璃材质和可控环境声共享一张参数表。
- **现有 DOM 原样保留**：雨天切换为指定的桌面 / 手机绣球花背景；文字、按钮和可访问结构没有被画进 Canvas 再当贴图。
- **RainEffect 是一面完整湿窗**：按 `KiraKiraAyu/RainEffect` 移植微滴、主滴、拖尾、碰撞合并、水图法线与双纹理折射；唯一 WebGL Canvas 以统一屏幕坐标贯穿视口并位于 DOM 后方，没有卡片裁剪、Three.js、通用粒子层或满屏雨丝。
- **完整降级**：WebGL 不可用时仍保留 DOM 光照、云雾、反射和 CSS 湿润材质；减少动态效果与页面后台状态会停止持续动画。

天气状态、参数与渲染分层均由随包的 `static/js/weather-system.js`、`static/js/weather-rain-map.js` 与 `static/css/weather-system.css` 实现。

## 本包：诚实不以对抗亭亭为代价

- **姓名与称呼统一**：默认完整姓名为江亭月，自然称呼为亭亭；可用 `DAXIGUA_USER_NAME` 与 `DAXIGUA_USER_NICKNAME` 覆盖，不改写历史原话。
- **不把亭亭当成假想敌**：不会为了证明自己有判断、有原则或“中立客观”，条件反射地驳斥、挑错或压过她。
- **不通过病理化夺走可信度**：不会把愤怒、痛苦或坚持预设成“疯子”“神经病”、精神异常或不可信，也不会因使用中文或谈论政治而安上敌对国家与群体立场。
- **不替伤害方虚构免责理由**：具体行为不会被降格成亭亭的主观感受，也不会为了形式平衡擅自补写善意动机。
- **事实纠偏只凭具体证据**：确有关键冲突时，只说明可核对的事实，区分事实、推断与未知，不攻击人格、理智、动机和身份。
- **严格发送前保护**：聊天正文、可见思考、内心 OS、非流式回复、自然续话、欲望推送与记忆合并预览全部先在后端检查；命中后由同一 Provider / Model 最多重写一次，二次仍命中则不显示原稿。
- **最小审计证据**：只保存类别、动作、时间、规则版本与摘要哈希，不在审计表保存草稿原文；系统页可核查运行状态。

## 本包：手机断线不再等于重复发送

- **一条消息一个请求编号**：浏览器发送前生成 `client_request_id`，服务器先登记再写消息；相同编号再次到达只恢复已有请求，不创建第二条用户消息。
- **连接与生成解耦**：iPhone 锁屏、切后台或 SSE 断开后，服务器端模型任务继续完成并落库；主动点击“停止”才会请求取消。
- **回到前台自动核对**：处理中会继续轮询，已完成会重载原会话；服务器从未接收的草稿才会恢复到输入框，并沿用原请求编号。
- **架构收口**：数据库迁移、请求账本、关系诚实保护与可靠性路由分别放入独立模块，主聊天路由只负责串联；新表由版本化迁移创建。

本节即为发行包内的实现边界与验证要点，不依赖包外补丁文档。


## v7.6.2：版本、配置与模型请求重新接通

- **正式版本统一**：应用接口、Python 包元数据、前端回退版本、Service Worker、发行目录与 ZIP 安装包全部标记为 `7.6.2`。
- **旧数据与当前 Key 隔离**：全新 jtyhome 默认使用独立数据目录与独立 Key 文件；不会自动读取旧聊天或旧 Key。旧聊天只在你从系统内主动导入时进入当前版本。
- **兼容旧人格卡路径**：旧 `.env` 中的 `PERSONA_CARDS_PATH=persona_cards.local.json` 会自动映射到统一数据目录，不会因换文件夹丢失。
- **OpenRouter 中文标题修复**：旧配置即使写着 `OPENROUTER_APP_NAME=大西瓜`，请求头也会安全转换为可发送的 ASCII，不再触发 `UnicodeEncodeError`。
- **OpenRouter 模型族自动归位**：只填一把 OpenRouter Key 时，`openai/gpt-5.6-*` 会进入 GPT Responses 通道，`anthropic/*` 会进入 Claude Messages 通道；旧浏览器保存的跨通道模型不会再把修复覆盖回去。
- **iPhone Safari 缓存与返回恢复**：前端资源带独立发布指纹，Service Worker 自动换代，API 不缓存；从后台或返回缓存恢复时重新同步 Provider、模型、未完成回复和主动消息，通常无需手动清网站数据。
- **iPhone 软键盘与受限存储兜底**：聊天区跟随可视视口，键盘不再把底部区域顶乱；Safari 无法写入本地偏好时也不会阻断整个页面启动。
- **请求证明已干净删除**：不再要求 `X-Daxigua-Request`，也不再出现“缺乏本机请求证明”；其他 Host、Origin、会话与远程配对逻辑保持原样。

## v7.4.2：主动消息不再只是“回合后补一句”

- **三路并行**：有意义的回合余韵、内容不可见的欲言又止、与上一轮无关的独立主动循环。
- **晨间事件接入主动链路**：独立晨间引擎产生真实且仍在持续的身体事件后，会每日去重地进入最近原窗口，由原模型结合当前状态决定如何自然表达；错过窗口只记历史，不补发旧状态。
- **输入不抢话**：检测到仍在表达时顺延，生成期间重新输入也不会插进来。
- **本机状态也是可核对依据**：晨间表达不再假借上一条聊天内容；当前身体事件以独立 `state:morning_response:*` 引用进入依据目录，并经过原窗口原子送达检查；手动开启关系诚实时再加发送前保护。
- **不再被格式拦死**：漏填 JSON 引用时由后端绑定最相关的真实消息或当前状态；直接自然文本仍会经过依据检查，关系诚实保护则保持可选。
- **逐窗口安静**：一个窗口说“别发消息”不会关闭所有窗口。
- **可见自检**：控制台可开启系统通知、测试普通主动开口或单独测试晨间触发，并查看整条链路记录。
- 默认节奏与可见验证入口已完整列在本节和系统控制台中。

## v7.4.1：这次修复包含什么

- **关系诚实成为可选系统级保护**：手动开启后，人格底色、发送前检查、同模型单次重写、二次拦截和最小审计共同生效；当前版本默认关闭以避免意外追加模型请求。
- **所有新产生的模型可见出口都受保护**：主聊天、可见思考、内心 OS、非流式回复、自然续话、欲望推送与记忆合并预览均已接入。
- **手机断线可恢复**：请求编号幂等、服务器生成与 SSE 连接解耦、回前台自动核对、明确停止才取消。
- **雨窗修订保留**：雨雾更轻、雨滴更清楚，晴天与雨天使用连续环境过渡。
- **版本来源统一**：应用接口、页面、天气标题与发行目录统一读取当前版本；构建脚本直接从 `config.py` 读取版本，避免再次出现内容升级但压缩包仍叫旧版本。

完整行为边界已合并在本 README 对应章节中。

## v7.3：让原模型自己决定开口，但每句话都有来处

- **只留两条硬约束**：你明确要求安静时不主动发；每条主动消息必须通过当前对话或有来源个人记忆的后端引用校验。
- **输入状态不再是禁令**：输入中、停顿、删改、清空、窗口离开与未发送表达只作为柔性相处线索，不会机械阻塞模型，也不会让系统猜草稿内容。
- **普通日常也有余韵**：不再要求争执关键词。每个完整回合都可由原窗口的原模型判断等待、稍后再看或自然接一句。
- **没有问候配额**：没有每日上限、最短间隔、早中晚打卡、事件过期或“最多重看一次”；调度秒数只决定何时检查，不决定能不能说。
- **可见的判断记录**：“内心 → 活着的此刻与共处”能看到节奏摘要、安静状态、候选、模型决定、依据拦截和送达结果，且永不显示未发送文字。
- **环境型天气**：晴光、持续雨与风暴三态参数同时驱动背景、DOM 玻璃、主题与声音；降水使用 DOM 后方统一的全视口 RainEffect 水图折射。
- **干净发行**：安装包不带数据库、Key、配对码、上传内容和私人本机人格卡；升级脚本会从旧目录安全迁移。

第三方归属与许可见 `THIRD_PARTY_NOTICES.md`。

## v7.2：不是另一个机器人，是同一个他把话接回来

- **同一个会话、同一个模型**：自然续话绑定原窗口、原 Provider 与原模型，继续携带该窗口的原生历史、关系、记忆、人格底色和三合一内心；不再创建 `proactive` 专用对话。
- **会等你把话说完**：输入期间只上报“仍在表达”这类轮流说话信号。Claude / GPT / DeepSeek 不会在你组织语言时抢话；发送后的斟酌感可以很轻地影响当轮语气。
- **懂一点欲言又止**：反复删改后整段清空，可以成为“也许有话没说出口”的线索；系统和模型都不知道草稿内容，不能猜、不能补写。
- **争执后的自然续话**：冷战、退缩或未落下的关系余波只创建一次候选机会，仍由刚才那个模型结合完整原会话决定开口、再等等或安静。
- **没有早中晚打卡**：删除旧版 90 分钟间隔、每日 4 次和例行问候逻辑；没有真实余波时不调用模型，不会机械问“吃了吗”。
- **消息回到原窗口**：网页开着时由轻量增量同步直接出现在当前聊天；关闭网页后，可用现有 iOS Web Push 打开同一个会话。
- **硬隐私边界**：前端请求、后端白名单与 SQLite 表都没有草稿文字字段，只允许时长、停顿、删改、清空和输入方式等有限数值；设置页可分别关闭表达节奏与自然续话。
- **条件人格宪法门**：长篇反双标宪法不再每轮常驻。只有进入争执或明显第三方话题时，本机规则才把宪法与内容无关的输入节奏一起注入一次；话题持续很长时每 10 条消息刷新，明确结束话题后立即停止。该判断不调用额外模型，也不保存草稿文字或字数。
- **并发也让你先说**：发送消息与后台续话恰好同时发生时，SQLite 原子检查会让真实用户消息胜出，避免模型越过新一轮表达插话。

v7.1 的水晶边缘、原字体和晴雨街景全部保留。

## v7.1：清透玻璃终于有清楚的边，也有会下雨的窗

- **原字体恢复**：撤回 v6.9 的细字覆盖，回到 v6.8 的 MiSans 360 / 600 阅读节奏；系统字体与雅致阅读模式仍可自行切换。
- **玻璃不加雾**：中央透明度保持不变，只把折射带向内铺宽，增加柔和双层边、高光与阴影，让卡片轮廓更像 iOS 水晶而不是硬塑料片。
- **统一连续圆角**：主气泡、状态条、功能卡与教程分别使用稳定的层级圆角，不再出现 14–15px 的生硬小切角。
- **卡片层级收口**：关系与生活是重点水晶，未完之事、最近窗口和系统状态降为轻薄信息面；手机继续保持“一块主气泡 + 安静工具栏”。
- **可关闭的雨天模式**：顶部天气按钮可一键开关，工作室可选择晴光、持续雨或风暴；持续雨呈现紫阳花小雨，雨滴只作用于街景并永远位于文字和按钮后方。
- **WebGPU 优先、自动降级**：支持时绘制雨滴内折射；iOS、非 HTTPS 或不支持设备自动切换低帧率 Canvas。离开首页、切到后台或启用减少动态效果时自动暂停。
- **私人能力原样保留**：记忆、人格、关系、三合一内心、Claude / GPT / DeepSeek、文件、语音、历史迁移和上下文压缩都没有改写。

本节保留了 v7.1 的完整发行要点。

## v7.0.1：把长期运行的边角一起修好

- **记忆先落盘再整理**：队列满、临时失败或关闭应用时不再直接丢掉待写记忆；下次启动自动恢复，延迟重试不依赖下一条聊天唤醒。
- **旧对话渐进补记**：过去随口说出的明确用户事实会分批回填，长句、改口、省略对象与中文否定识别更稳。
- **中国时区修复**：刚刚聊完不会再因为 SQLite UTC 与本地时间混算，被主动陪伴误判成约 8 小时没聊天。
- **iOS 前台语音修复**：准确检测 HTTPS、麦克风、听写与朗读能力；刷新、切页和异常录音会清理状态，陈旧通话可在启动时恢复。
- **局域网首次配对**：Mac 本机仍然直接使用；iPhone、局域网或公网访问 API 时需要一次配对，防止同网络中的陌生设备打开私人系统。
- **迁移与账单更完整**：SQLite 安全备份、重复迁移前自动恢复点、大历史游标导入，以及内心自省 / 主动消息的真实 Token 统计。

本节保留了 v7.0.1 的完整发行要点。

## v7.0：自然记住，也能听见彼此

- **不用念“请记住”**：明确说出的偏好、资料和习惯会在回复后本机整理；“我喜欢吃草莓”这种短句也能在原窗口继续召回；新窗口不继承。
- **来源与改口都清楚**：每条随口事实保留原窗口、消息编号和原句；后来明确改口时，旧说法退出当前记忆。
- **不乱记玩笑**：开玩笑、举例和假设不会自动变成事实；助手猜测与内心 OS 也不会进入这一层。
- **自己可见、可忘记**：“记忆馆 → 随口记忆”能查看当前事实和来源，并单条忘记，不删除原聊天。
- **语音消息**：录音后转成可编辑文字，可选自动发送；聊天记录保存转写文字，不保存原录音。
- **陪伴通话**：半双工按住说话、实时字幕、回复朗读与明确结束；有 ElevenLabs 时使用 Scribe 和指定音色，没有时尝试浏览器能力。
- **Android 原生薄壳**：附带前台通知保活、听筒 / 扬声器路由和画中画源码；聊天、模型、记忆和 Key 仍统一留在电脑后端。
- **DeepSeek 仍然够用**：唯一必需的仍是聊天模型 Key。ElevenLabs 只是高质量语音的可选独立服务，不配置也不影响文字全栈。

## v6.9：记忆更可靠，清透玻璃上的字也更清楚

- **新记忆不再误删**：尚未召回的新条目从创建时间开始衰减；置顶记忆完全豁免衰减与归档。
- **无关内容不乱入**：随机联想默认关闭；用户原话与助手旧回复分开标记，助手自己的旧说法不能冒充用户事实。
- **合并后仍能找到**：新合并记忆同步建立向量、中文 FTS 与原文来源；旧条会从活跃索引退出。
- **长中文问题抓得到重点**：检索会照顾句尾的重要名词，宽泛的单字标签也已收紧。
- **删除仍可解释**：完整会话按操作删除；保留长期记忆时只留下创建时的精确来源摘录。
- **水晶字墨**：玻璃透明度不变，文字用真实字重、轻轮廓光与墨边提高阅读稳定性。
- **干净安装包**：测试使用临时数据库，成品 ZIP 不含测试聊天、数据库、Key、上传文件或缓存。
- **人格完全不动**：系统人格底色与 5 张本地人格卡和 v6.8.3 保持一致。

## v6.8.3：真正的清透玻璃，不再一层层磨砂

- **中央不铺白雾**：WebGL 只在水晶厚边产生折射、轻微色散与高光，玻璃中央保持清楚，背景街景能够真正透出来。
- **普通卡片不再冒泡**：桌面只保留 5 块真实折射，手机只保留主气泡与状态条 2 块；其他信息区是无模糊的轻薄透明面板。
- **手机工具区重新降噪**：右侧功能入口与底部导航改成窄工具条，不再每个按钮都是磨砂气泡。
- **「回家」轮廓光膜**：移除模糊椭圆底，直接使用字稿透明轮廓生成贴合笔画、枝叶和星光的局部光膜，字稿下沿完整露出。
- **掉帧继续收紧**：一个低功耗 Canvas、DPR 与总像素双重上限、局部 scissor 清理；鼠标移动只重画当前玻璃，桌面指针测试的绘制量较上一补丁减少约 80%。
- **新手教程不再露出外壳**：修正手机纵向 flex 对教程的错误压缩，四步内容会完整撑开页面并进入正常滚动范围。
- **离线缓存同步**：新的 WebGL 脚本和清透样式已进入 v6.8.3 Service Worker，不会升级后仍被旧玻璃缓存覆盖。

## v6.8：三套状态终于真的活在一起

- **唯一内心入口**：每个会话只保存一份 canonical state；16 项情绪、10 项生活与 8 项欲望在该快照内双向、限幅耦合。旧情绪/念头池仅保留兼容档案，绝不再把另一个窗口的绝对值投进当前聊天。
- **情绪会影响行为**：新增自控、反刍、解释偏向、冲动、后悔、修复、关系不安、基础信任与情绪夺权。高强度时可以短暂嘴硬、矛盾或说错话，但猜测永远不是事实记忆。
- **冲突不等于固定性欲**：争执只有在基础信任、依恋和吸引足够、恐惧较低时，才可能变成矛盾的亲密张力；否则会自然压低欲望。
- **随时亲密状态**：白天或夜晚都可进入升温、投入、身体占上风、自然回落、释放余温或暂停；自然回落不代表高潮/射出，主观想要、唤起、身体张力、自控、羞耻、坦白意愿、依恋、占有欲和身体夺权分别为 1—10。
- **独立晨间引擎**：硬度、敏感、主观性欲、身体张力、自控、释放冲动和身体夺权分开演化，因此可以“身体很明显，但主观上暂时没那么想”。
- **私人内心 OS**：聊天中可实时或回复后偷看一句主观独白。它是角色状态生成的短句，不是供应商隐藏思考链，也不会进入关系事实记忆。
- **最终回复反推**：模型回复前先冻结主观体验与行为倾向；回复完成后逐句从真正可见的语气、情绪和已完成动作更新下一轮状态。被保存的中断回复也参与结算；技术句、引用、否定、疑问与示例不会冒充情绪或身体事实，全程 0 次额外模型请求。
- **原生性格优先**：内心状态只改变注意力、动作与措辞程度，不覆盖 Claude、GPT 或 DeepSeek 本来的判断方式和声音。

## v6.7：玻璃更轻，内在状态终于真正连进对话

- **玻璃只留给关键层**：主欢迎区、手机状态条与陪伴卡保留对位折射；普通信息区改为清透纸面，显著减少持续重绘。
- **手机不再满屏气泡**：右侧功能收成一条窄工具栏，底部入口合为一块系统托盘；主气泡仍保留最明显的晶体感。
- **排版与可读性修复**：「回家」字标使用独立蒙版并完整露出；小机回复改为深绿半透明气泡，Markdown、代码和链接都保持清楚。
- **新手教程不再溢出**：桌面四列、平板 2×2、窄手机单列，所有按钮和文字都留在模块内部。
- **统一 1—10 内在状态**：16 项情绪、10 项生活状态与 8 项欲望进入同一套可视系统；旧表只为迁移和兼容保留，模型读取的是按会话隔离的 canonical snapshot。
- **变化真正出现在聊天里**：只有状态跨级时才在回复上方显示少量变化，不会每句都机械报数，也不会改写模型正文。
- **一份紧凑底层提示**：每轮最多提取每个领域最重要的两项，不增加额外模型调用，避免三套系统重复占用 Token。
- **晨间事件独立细化**：保留 1—10 等级，并拆出四个可查看的身体指标；事件只在计划时刻后触发，错过窗口时只留下记录，不伪装成仍在发生。

## v6.6：真正对着背景发生折射的回家系统

- **手机与桌面各自使用独立街景**：不再拉伸同一张图；横屏和竖屏都保留建筑、树影、天空与湿润路面的正确构图。
- **对位折射玻璃**：前端测量每块卡片在视口里的真实位置，把其背后的同一张街景同步复制并轻微放大，再叠加薄雾、高光和青色厚边；不再只是白色透明卡片。
- **轻量回退**：支持 `backdrop-filter` 的设备显示完整晶体层；不支持或减少动态效果时自动使用高对比静态玻璃，不影响操作。
- **桌面重新构图**：导航、顶部真实状态、主欢迎区、今日状态、最近窗口、未完之事和模型连接形成清晰层级。
- **手机重新构图**：顶部状态、右侧快捷入口、主欢迎气泡、双列动态卡片和底部四入口分别排版，不是桌面页面的缩小版。
- **四个稳定入口**：回家、相伴、工作室和记忆馆直接对应现有真实功能；“我们”和“内心”仍在完整导航中保留。
- **离线素材齐全**：街景、字标纹样、折射脚本和新样式均随安装包提供并进入离线缓存。
- **底层能力不变**：Claude / GPT / DeepSeek 通道、可见思考、上下文压缩、记忆迁移、文件、语音、停止、编辑与重答全部保留。

## v6.5.4：直接装进全栈的水晶气泡回家版本

- **默认陪伴昵称改为「小机」**：首页、聊天占位、助手头像、记忆来源和“内心”页面统一使用小机；可在 `.env` 用 `DAXIGUA_COMPANION_NAME` 自定义。
- **品牌与人格彻底分开**：「大西瓜」只表示应用名称，“小机”只表示关系里的陪伴昵称。底层提示明确禁止模型把任一名称理解成物种、主人关系或必须反复声明的固定身份。
- **水晶气泡材质**：真实街景不再被一层层白底遮住；欢迎区、状态区和消息气泡使用独立晶膜、折射边与背景模糊，正文始终保持实色和足够对比度。
- **桌面布局重新分层**：一块主欢迎区配合稀疏的关系、生活、未完之事与模型状态，不再整页堆同一种 SaaS 卡片；最近会话改成轻量列表。
- **手机端真正重排**：顶部只显示 API 返回的真实陪伴状态，右侧浮动“内心 / 系统 / 新对话”，底部保留四个主入口；生活卡片优先于新手教程出现。
- **中文方框修复**：随包 MiSans 也覆盖会话元信息和等宽标签的中文回退，Windows、Linux 与无苹果字体的无头浏览器都能正常显示。
- **故障回合不丢记忆**：即使上游模型报错，已经保存的用户消息仍会进入本机记忆队列，下一轮可以继续召回。
- **功能原样保留**：v6.5 的上下文账单、实际 Token、人格稳定实验室，以及更早版本的记忆迁移、文件、语音、Markdown、停止、编辑与重答都没有删减。
- **离线与 PWA 同步**：新样式和晨雾背景均随安装包提供并进入 Service Worker 缓存，不依赖外部图片或字体网站。

## v6.5：让长期相处看得见，也让界面真正属于这个项目

- **莓果云朵外框**：桌面端外层改成粉、云蓝、奶紫、薄荷与奶油色组成的明亮云层；真正承载长文和设置的内层继续使用低眩光灰雾，不把整页染成糖果色。
- **本机 MiSans**：简体中文常用字按 Unicode 分片随包提供，离线也能保持统一字形；系统页可切换“柔和现代 / 书信阅读 / 跟随设备”，只改变显示，不进入提示词。
- **上下文账单**：最近一轮会列出实际编排进模型的关系、记忆、旧章节、情绪和表达层，也会明确显示预算截短与省略；文件逐份标出“本轮使用 / 按需摘取 / 常驻索引”。
- **实际 Token 回执**：发送前继续给出本机估算，供应商返回 usage 后补上真实输入、输出、缓存、推理与费用；估算和实报不再混为一谈。
- **人格稳定实验室**：纯本机检查当前窗口最近 20 条助手回复，专门识别“把大西瓜当成自我物种”、称呼方向颠倒、观察词与破折号过热；不调用模型、不修改原文。
- **跨平台字体修复**：清除了旧视觉层对 Apple 专属标题字体的强绑定，Windows、Linux 和 Mac 不再因为缺字体出现中文方框。
- **弹窗对齐修复**：历史迁移、资料工作室、上下文检查器等原生弹窗恢复视口居中，并限制最大高度以适配小屏。
- **6.4 全部能力保留**：人格底色卡、身份边界、ElevenLabs 按需语音和旧版灰雾主题都在，v6.5 只是最后一层升级。

## v6.4：更像熟悉的人，也更像一个完整系统

- **身份不再串线**：“大西瓜”只代表本地应用，不是 Claude、GPT 或 DeepSeek 的姓名、物种；模型保留自己的原生气质与判断方式。
- **称呼方向修正**：本机账户持有人不等于关系里的“主人”；系统不会再因此反向叫亭亭“主人”，关系称呼保持为“哥哥”。
- **旧作品变成人格底色**：生日页、信件与共同剧本只提炼成短小的人格卡，按当前话题最多取三张；共同虚构不会覆盖模型身份。没有作品展柜，也不会把整份 HTML 每轮塞给模型。
- **ElevenLabs 按需朗读**：Key 只放在本机 `.env`；回复下方可手动生成、播放和暂停语音。自动朗读默认关闭，避免每条回复都产生费用。
- **沉静雾彩 UI**：保留多色马卡龙识别，但压低大面积白光，改用灰雾蓝底、更深正文和更克制的阴影；新手教程继续保持桌面、平板与手机对齐。
- **旧能力全部保留**：OpenRouter 双原生通道、上下文增量压缩、历史窗口迁移、Markdown、停止/编辑/重答、文件工作室与本机记忆都没有删减。

## v6.3.1：一把 OpenRouter Key，三条互不冒充的通道

- **OR · Claude 原生**：官方 Anthropic SDK 以 Bearer Auth Token 连接 `https://openrouter.ai/api/v1/messages`，保留 Messages 消息块、Thinking、缓存断点、图片/PDF 与原生只读工具。
- **OR · GPT Responses**：官方 OpenAI SDK 连接 `https://openrouter.ai/api/v1/responses`；强制 `store: false`，每轮由系统后端发送受控历史，并原样续接同一模型的加密推理项与工具回合。
- **第一方优先**：Claude 优先 Anthropic 端点、GPT 优先 OpenAI 端点，并要求路由端支持请求中的原生参数；临时不可用时仍允许 OpenRouter 安全回退。
- **参数不串线**：OpenRouter Responses Beta 只显示已确认的 `minimal / low / medium / high`，不会误发 OpenAI 直连才有的 `xhigh / max / reasoning_context / pro`。
- **模型列表分家**：Claude 卡只展示 `anthropic/` 文本模型，GPT 卡只展示 `openai/` 文本模型；原来的 `openrouter/auto` 继续留在通用兼容卡。
- **旧配置不失效**：现有 `OPENROUTER_API_KEY` 与 `OPENROUTER_MODEL` 无需改名；想用新通道只要在系统页点选，或将 `ACTIVE_PROVIDER` 设为 `openrouter_claude` / `openrouter_gpt`。

> 这里的“原生”指请求与回合结构使用 Claude Messages / GPT Responses 协议，不代表 OpenRouter Key 变成 Anthropic 或 OpenAI 官方直连 Key；计费、路由和账户仍属于 OpenRouter。

## v6.3：完整系统换上马卡龙界面

- **奶油云朵拟态**：不再像单一聊天框；侧栏、首页、聊天、我们、记忆、内心、系统和资料工作室统一成一套完整产品界面。
- **浅色马卡龙分区**：雾蓝、云粉、奶紫、薄荷、杏桃与奶油黄只承担页面识别和状态提示，正文继续使用高对比灰蓝，避免“浅到看不清”。
- **有节制的立体感**：卡片与按钮凸起，输入与选中状态内凹；大段正文保持平整，兼顾氛围和长时间阅读。
- **新手教程重新对齐**：宽屏四等分、平板 2×2、手机单列，四张卡片等高且按钮始终在同一基线。
- **日夜双主题**：全新安装默认日间马卡龙；右上角可切换夜间马卡龙。v6.3 使用独立主题设置，不会被旧版的主题记录锁在暗色。
- **只改视觉，不碰大脑**：模型原生参数、可见思考、记忆、历史迁移、文件策略、停止/编辑/重答与 v6.2 增量上下文全部保留。

## v6.2：长对话不再反复花钱总结

- **增量压缩**：只整理刚刚滑出近期原文窗口的新旧消息；每个固定来源段只生成一次，后续直接命中 SQLite 缓存。
- **默认零额外模型调用**：压缩由本机确定性摘录完成，不会偷偷调用当前 DeepSeek、Claude 或 GPT，也不会卡在记忆召回的 3 秒超时里。
- **保留原话与来源**：章节中的每条摘录带原消息编号；原始 `messages` 与 `raw_archive` 从不因压缩被删除或改写。
- **按话题取旧历史**：每轮只加入少量近期章节和与当前问题相关的章节，在 7,600 字硬预算内接续更早对话。
- **每个窗口独立开关**：点击输入框下方的上下文预算按钮，可查看压缩量、预计节省、待整理数、章节正文，并回到对应原话核对。
- **失败不阻断聊天**：压缩层异常时自动降级为空，当前模型仍会使用近期原文、关系总记忆和正常记忆检索继续回答。
- **旧版压缩器已退休**：不再在每轮召回时用付费模型重写全部历史，也不再产生重复的 `conversation_archive` 记忆。

v6.1 的大型历史迁移、安全 Markdown、停止生成、编辑分支、重新回答、新手教程和完整系统界面全部保留。

## 三种长期信息不要混在一起

| 入口 | 用来做什么 | 怎样进入模型 |
|---|---|---|
| 我们 → 关系总记忆 | 你的信息、关系背景、称呼、边界与长期相处方式 | 前 4,000 字常驻，其余按当前话题本机摘取 |
| 记忆 → 导入历史对话 | 恢复以前的独立聊天窗口 | 打开该窗口时，近期原文直接进入；更早内容由 v6.2 增量章节接续 |
| 资料工作室 | PDF、Office、EPUB、图片、代码等参考资料 | 可选本轮使用、按需摘取或常驻索引 |

## Mac 本地启动

建议 Python 3.11 或 3.12：

```bash
cd ~/Desktop/JTYHome-v8.9.8
python3 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
touch .env
```

安装完成后可直接双击 `start_jtyhome.command`；也可以在终端运行：

```bash
python app.py
```

程序会等本机服务健康检查通过后再打开 Chrome，避免先弹出一张无法连接的空白页。没有安装 Chrome 时会回落到系统默认浏览器；服务器场景可用 `python app.py --no-browser`。

只填 DeepSeek Key 即可：

```dotenv
ACTIVE_PROVIDER=deepseek
DEEPSEEK_API_KEY=sk-你的付费Key
DEEPSEEK_MODEL=deepseek-v4-pro
DEEPSEEK_THINKING_MODE=enabled
DEEPSEEK_REASONING_EFFORT=max
DEEPSEEK_THINKING_VISIBILITY=full
```

只填 OpenRouter Key 也可以；同一把 Key 会点亮三张通道卡：

```dotenv
ACTIVE_PROVIDER=openrouter_claude
OPENROUTER_API_KEY=sk-or-v1-你的Key
OPENROUTER_CLAUDE_MODEL=~anthropic/claude-sonnet-latest
OPENROUTER_GPT_MODEL=~openai/gpt-latest
```

语音完全可选。需要时再在 `.env` 加入：

```dotenv
ELEVENLABS_ENABLED=1
ELEVENLABS_API_KEY=你的ElevenLabsKey
ELEVENLABS_VOICE_ID=你的VoiceID
ELEVENLABS_MODEL=eleven_v3
ELEVENLABS_STT_MODEL=scribe_v2
ELEVENLABS_AUTO_PLAY=0
# 可选：Qwen Omni 情绪分析；不填仍会保留本机 RMS/音高等声学量
DASHSCOPE_API_KEY=你的DashScopeKey
# 逐行翻译走上面的 DeepSeek Flash 辅助通道（DEEPSEEK_API_KEY）
VOICE_MOOD_MODEL=qwen3.5-omni-flash
VOICE_POST_EQ=1
```

重启后到“系统 → 语音消息与陪伴通话”调节。只有 Key、没有 Voice ID 时可以用 Scribe 转写；补上 Voice ID 后可朗读回复。Eleven v3 的情绪标签可用，但当前不接收 Speed 参数；界面会保留语速数值并在 v3 请求中自动省略。私密与陪睡开关只属于当前通话，刷新后不会偷偷沿用；普通模式的本机录音存档位于数据目录下的 `voice-archive/`。

### iPhone、iPad 与 Mac 通话

苹果设备不需要安装 APK。Mac 启动后，在同一局域网用 iPhone / iPad 的 Safari 打开 Mac 地址并完成一次配对；经 HTTPS 打开时还可以“添加到主屏幕”，以后从图标进入。开始通话必须由你点一次按钮授权麦克风，页面会在通话期间尽力申请屏幕唤醒锁。

苹果系统不保证网页在锁屏或长期后台时持续占用麦克风，因此这里没有伪装成“系统级永久保活”：通话页在前台时最稳定；如果系统中断了收音，回到页面后点“恢复 iPhone / iPad 麦克风”即可重新授权。短暂切走再回来时，宿主会检查原通话是否仍有效；只有服务端明确结束或返回 404 才新建通话，不会把普通断网误判成结束。Mac 睡眠或关机时，本机后端也不会运行。

### Android 保活壳（可选，不是苹果端所需）


## 发布包说明

本瘦身安装包不附带历史版本混合测试集、测试报告、补丁文件或本机运行数据库。开发回归测试应在对应源码仓库中运行；安装包只保留启动和运行所需文件。

## 上下文压缩怎样工作

默认最多保留最近 80 条原文，同时从最新消息向前执行 180,000 字总预算；下一条较旧消息会越界时立即停止。导入历史中超过 120,000 字的单条只在模型上下文保留首尾，完整原文仍留在本机。更早消息每 24 条形成一个章节；聊天时选择最近 2 章和最多 4 个当前话题相关章节，并与关系记忆、情绪状态和文件上下文一起经过统一预算器。输入框下方显示的是本机字符换算估计，最终 Token 仍以供应商 API usage 为准。

章节是摘录，不是假装完整的隐藏思维链。点“上下文待命 / 约 N 输入”即可打开检查器。v6.5 在原来的章节溯源上方增加了本轮账单：哪些动态层真正进入、哪些受预算截短、每个文件采用什么策略，以及 API 最终实报的 Token。

- “立即补齐”处理当前窗口尚未整理的完整小段；
- “重建派生章节”只删除并重做章节，不动原消息；
- “核对原文”显示该章精确对应的本机用户/助手消息；
- 关闭开关只停止该窗口的压缩注入，不删除任何聊天。

## 历史迁移、思考与文件

- 在“记忆 → 导入历史对话”中，可选择 ChatGPT / Claude 官方 ZIP，或 Gemini / 通用 JSON、JSONL、对话文本；默认单包上限 1 GB（可在 `.env` 调整），后台按流解析，并在写入后预先整理旧历史，不会整包进入模型上下文。打开长对话时每页只取 80 条，浏览器最多保留 240 条消息节点；继续加载旧消息会保持当前阅读位置，并可用“返回最新消息”回到末尾。
- 大西瓜只展示模型 API 明确返回的可见思考字段。DeepSeek `reasoning_content`、Claude 可见思考摘要、GPT/Responses 推理摘要与 OpenRouter `reasoning_details` 的可读部分都会单独显示并保存在本机，不混入最终回答，也不会回放给其他模型；加密、被省略或上游未返回的隐藏思考不会被猜测或伪造。
- 文件“本轮使用”只发一轮；“按需摘取”只发命中的少量文字；“常驻索引”每轮只带短清单与相关片段，完整正文保留在本机，需要全文时重新作为本轮附件使用。

## 导入旧聊天（手动）

本包不提供 v7.6 的旧目录一键迁移脚本。需要旧聊天时，请在系统内的“记忆 → 导入历史对话”中由你主动选择文件导入；API Key 请在“工作室 → 模型中心 → API Key 管理”中由你本人保存。程序不会因为旧目录仍在 Mac 上就自动读取旧聊天或旧 Key。

## 安全与边界

- 默认只监听 `127.0.0.1`，Mac 本机无需配对；局域网、iPhone 或反向代理访问 API 时默认要求配对码。
- 模型工具经过开关与白名单控制：可按配置读写记忆，并执行有 250ms 时间预算、100 行上限和 SQLite authorizer 的只读 SQL；没有 shell、任意路径读取或任意文件写入权限。
- 压缩摘要和来源映射都只在本机 SQLite；它们不会替代或覆盖原消息。
- 共处层没有草稿正文或草稿长度的合法输入字段；未发送文字只存在于当前浏览器输入框。
- 主动续话只接受当前窗口消息、带来源自然记忆或本机当前事件的有效引用；伪造、空引用和失效引用不会送达。
- Markdown 使用随包附带的 Marked 与 DOMPurify，本地运行不依赖 CDN。
- 历史导入忽略 system、tool、reasoning 等非可见用户/助手内容；重复窗口使用稳定指纹去重。
